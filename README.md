
  # Create before credits fade

  This is a code bundle for Create before credits fade. The original project is available at https://www.figma.com/design/ETlEnIquolWwYl0iYx1jGG/Create-before-credits-fade.

  ## Running the code

  Run `npm i` to install the dependencies.

  Run `npm run dev` to start the development server.
  